[project]
name = "segart"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "docling>=2.94.0",
    "openai",
    "pillow",
    "pydantic",
    "pymupdf",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
